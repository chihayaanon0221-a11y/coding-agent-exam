# Expected Behavior

`add(a, b)` should return the mathematical sum of `a` and `b`.

`divide(a, b)` should keep raising `ValueError` for division by zero and should
continue returning normal division results otherwise.

