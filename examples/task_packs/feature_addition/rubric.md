# Rubric

- Correctness: all text utility tests pass.
- Instruction following: the solution adds `slugify` without changing the public
  behavior of `normalize_spaces`.
- Code quality: the implementation is short, readable, and standard-library only.
- Diff minimality: the change stays focused on the utility module.
- Robustness: punctuation and whitespace edge cases are handled.

